<% @patient.doctors.each do |doctor| %>
<%= link_to doctor.name, doctor_path(doctor)%>
<%= doctor.appointments.appointment_datetime.strftime("%B %-d, %Y at %H:%M")
<% end %>

<% @patient.appointments.each do |appointment| %>